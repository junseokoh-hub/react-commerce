import React from "react";
import { useTitle } from "../../hooks/useTitle";

const ReviewPage = () => {
  useTitle("Review");

  return (
    <article>
      <ul>Hello</ul>
    </article>
  );
};

export default ReviewPage;
